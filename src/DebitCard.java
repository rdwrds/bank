public class DebitCard extends Card {
    //the debit card extends the functionality of a card
    //but since is has nothing unique to it, it can use all the super class functionality.
    public DebitCard()
    {
        super();
    }
}
